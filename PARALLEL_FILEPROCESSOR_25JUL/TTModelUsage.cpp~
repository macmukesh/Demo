#include<iostream>
#include "TTModel.h"

using namespace std;

enum parsercd
{
  HDR,
  TRL,
  DATA1,
  DATA2,
  DATA3,
  DATA4,
  DATA5	
};

typedef struct
{
  int ix;
}Data;

int main()
{
	cout<<"Sizeofdata:"<<sizeof(Data)<<endl;
	Model ObjModel;

	char acdata[19+1];
	memset(acdata,0x00,sizeof(acdata));
	strcpy(acdata,"0AS1234000000000000");

	bool bret=ObjModel.SetData(StructTTUB::MEID,acdata,strlen(acdata));

	cout<<"Return:"<<bret<<endl;
	cout<<"Sizeof class:"<<sizeof(Model)<<endl;
	cout<<"Sizeof struct:"<<sizeof(StructTTUB)<<endl;
}
